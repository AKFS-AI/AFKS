Title: Core - Hierarchy & Wiring Guide
SceneName: Core
Purpose: 전역 서비스/전환 페이드/초기 로더 상주(씬별 MainCanvas 정책 적용)

1) 하이라키어 최종 구조도
- Core_Empty(-)
  - Services_Empty(-)
    - AudioService_Empty(AudioService)
    - InputService_Empty(Input Service)
    - SceneService_Empty(SceneService)
    - InventoryService_Empty(InventoryService)
    - StartupLoader_Empty(StartupLoader)
  - UI_Empty(-)
    - FadeCanvas_UI/Canvas(Canvas, CanvasScaler, GraphicRaycaster, Image, CanvasGroup, FadeCanvas)
  - Empty_Empty(-)
    - SaveServiceBootstrap_Empty(-)
  - Utils_Empty(-)
    - GlobalSingletonGuard_Empty(GlobalSingletonGuard)

2) 하이라키어 제작 절차(생성 경로 포함)
- 컨테이너: Services_Empty, UI_Empty, Empty_Empty, Utils_Empty
- Services 구성:
  - AudioService_Empty(AudioService)
  - InputService_Empty(Input Service)
  - SceneService_Empty(SceneService)
  - InventoryService_Empty(InventoryService)
  - StartupLoader_Empty(StartupLoader)
- UI 구성:
  - FadeCanvas_UI/Canvas(Canvas, CanvasScaler, GraphicRaycaster, Image, CanvasGroup, FadeCanvas)
- Empty 구성:
  - SaveServiceBootstrap_Empty(-)
- Utils 구성:
  - GlobalSingletonGuard_Empty(GlobalSingletonGuard)

3) 인스펙터 설정(실제 인스펙터 순서)
- Services/InputService (Empty)
  - Input Service(스크립트)
    - UI 카메라 = None(Camera, 옵션, 스테이지 로드시 자동 바인딩)
    - UI 위 클릭 무시 = On(권장)

- Services/SceneService (Empty)
  - Scene Service(스크립트)
    - 페이드 캔버스 = Core/UI/FadeCanvas의 FadeCanvas
    - 기본 페이드 시간(초) = 0.60

- UI/FadeCanvas (Canvas + Image + CanvasGroup + FadeCanvas)
  - RectTransform: Stretch(Left/Right/Top/Bottom=0)
  - Canvas: Render Mode=Overlay, Sorting Order=1000
  - Canvas Scaler: 1920x1080, Match 1.0(Height)
  - Image: Color #000000, Alpha 1.0
  - Canvas Group: Alpha 0, Interactable Off, Blocks Off
  - Fade Canvas(스크립트): 캔버스 그룹 = 자기 CanvasGroup

- Utils/GlobalSingletonGuard
  - GlobalSingletonGuard(스크립트)
    - EventSystem 단일 활성 = On
    - AudioListener 단일 활성 = On

4) Components & Wiring(참조/이벤트)
- GameEvents.StageChangeRequested → SceneService.OnStageChangeRequested
- SceneService: Fade.Out → LoadAdditive(next) → Activate → Unload(prev) → Fade.In
- FadeCanvas API: SceneService에서 전환 시 호출
- InputService: ObjectClicked 이벤트로 2D/3D 클릭 타겟 전달, UI 위 클릭 무시(옵션)
- InventoryService: PickupItem/ DoorLocked 등에서 조회/수정 사용
- GlobalSingletonGuard: 씬 공존 시 EventSystem/AudioListener 중복을 자동 비활성화하여 1개만 활성 유지

5) Build Settings(Scenes In Build)
- Core.unity를 목록 최상단에 배치(항상 상주)
- MainMenu, Stage1, Stage2 등록(추가 스테이지는 이후 순서대로)

6) Tests(기능 검증)
- 재생 → StartupLoader가 Initial Stage 요청 → SceneService 전환(3회 연속) 성공
- 전환 중 연타 입력 무시(재진입 가드)

7) 체크리스트
- [ ] FadeCanvas 참조가 SceneService에 연결됨
- [ ] InventoryService 존재(필요 시)
- [ ] EventSystem은 스테이지 씬마다 1개만(중복 시 GlobalSingletonGuard가 비활성화)
- [ ] 활성 AudioListener 1개(Main Camera 권장)
- [ ] Build Settings 순서(Core → MainMenu → Stage1 → Stage2) 확인

8) FAQ(문제 해결)
- 클릭 불가: 각 씬의 EventSystem 유무, Collider2D/카메라 레이어 확인
- 전환 실패: 씬 미등록/ID 오타 로그 확인, StageIds↔씬 이름 일치
- 오디오 경고: AudioListener가 중복되지 않았는지 확인(활성 1개)

9) 변경 이력(Change Log)
- v1.2: 전역 UI 제거(씬별 MainCanvas 정책), FadeCanvas만 Core에 유지